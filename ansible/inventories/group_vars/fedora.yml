# Group variables for the 'fedora' Ansible inventory group.
# Define here variables that should be shared across all fedora-based hosts.
# These settings apply to all hosts in the 'fedora' group.
# For host-specific values, use '<host>/host_vars/<host>.yml'.

ansible_python_interpreter: /usr/bin/python