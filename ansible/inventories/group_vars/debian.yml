# Group variables for the 'debian' Ansible inventory group.
# Define here variables that should be shared across all debian-based hosts.
# These settings apply to all hosts in the 'debian' group.
# For host-specific values, use '<host>/host_vars/<host>.yml'.

ansible_python_interpreter: /usr/bin/python3