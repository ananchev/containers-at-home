# Group variables for the 'ubuntu' Ansible inventory group.
# Define here variables that should be shared across all ubuntu-based hosts.
# These settings apply to all hosts in the 'ubuntu' group.
# For host-specific values, use '<host>/host_vars/<host>.yml'.

ansible_python_interpreter: /usr/bin/python3
docker_data_root: /opt/docker